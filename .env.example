# Set these in Vercel Project Settings > Environment Variables.
# Values are comma-separated and are never exposed to the browser bundle.
# Access codes are case-sensitive; keep the intended capitalization.
ACCESS_CODES_ALL=
ACCESS_CODES_H=
ACCESS_CODES_GN8=
